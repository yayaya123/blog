<%= form_for(@voiture) do |f| %>
  <% if @voiture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@voiture.errors.count, "error") %> prohibited this voiture from being saved:</h2>

      <ul>
      <% @voiture.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :marque %><br>
    <%= f.text_field :marque %>
  </div>
  <div class="field">
    <%= f.label :annee %><br>
    <%= f.number_field :annee %>
  </div>
  <div class="field">
    <%= f.label :Pilote_id %><br>
    <%= f.text_field :Pilote_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
